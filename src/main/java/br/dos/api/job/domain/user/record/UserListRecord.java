package br.dos.api.job.domain.user.record;

public record UserListRecord(Long id,
                             String name,
                             String cpf,
                             String neighborhood) {
}
